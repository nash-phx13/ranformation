  <div class='container '>
    <h2>
      <i class="fas fa-address-card fa-lg"></i>プロフィール
    </h2>
    <%= render 'info', user: @user %>
  </div>
  <div class='container '>
    <h2>
      <i class="fas fa-bookmark fa-lg"></i>投稿一覧
    </h2>
    <%= render 'run_index',runs: @runs %>
  </div>
  <div class='container '>
    <% if @user == current_user %>
      <h2 class="row-md-3">
        <i class="fas fa-folder-plus fa-lg"></i>新規投稿
      </h2>
      <%= render 'runs/form', run: @run %>
    <% end %>
  </div>
